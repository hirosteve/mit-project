# Requires Python 3.10 or newer (3.12 recommended).
# The bundled model artefact was serialised with the versions below; installing
# markedly older scikit-learn or xgboost will fail to unpickle it.

Django>=4.2,<6.0
whitenoise>=6.6.0
scikit-learn>=1.5
imbalanced-learn>=0.12
xgboost>=2.0
shap>=0.45
pandas>=2.0
numpy>=1.26
joblib>=1.3
gunicorn>=21.0
